# GX教育项目交付管理系统

**版本**: V2.2
**更新日期**: 2026-07-29
**仓库**: git@github.com:wtpan3/gx-project.git (Private)


## 📋 目录

- [完成的定义](#完成的定义definition-of-done必读)
- [项目简介](#项目简介)
- [快速启动](#快速启动)
- [核心约定](#核心约定)
- [架构决策](#架构决策)
- [数据库表结构](#数据库表结构)
- [API接口规范](#api接口规范)
- [开发进度](#开发进度)
- [重要踩坑记录](#重要踩坑记录)
- [Git工作流](#git工作流)
- [文档索引](#文档索引)
- [快速检查清单](#快速检查清单)
- [已知问题](#已知问题待修复)
- [技术支持](#技术支持)


## 完成的定义（Definition of Done，必读）

> 这是本项目所有开发、修改、部署工作的**总原则**，优先级高于一切其他约定。

**任何工作，必须用「真实使用场景」验证通过，才能称为"完成"。**

### 什么不算"完成"

- ❌ "代码写完了" —— 那只是写完，不是完成
- ❌ "接口 curl 返回 200 了" —— curl 绕过浏览器的 CORS/JS/连锁请求，测不出真实问题
- ❌ "服务起来了 / 编译过了" —— 进程在跑 ≠ 用户能用

### 什么才算"完成"

**用真实场景走通**：像真实用户那样，在浏览器里操作完整路径，确认无误。

| 场景               | "完成"的验证标准                                       |
| ------------------ | ------------------------------------------------------ |
| 开发环境改了某功能 | 浏览器打开对应页面，实际操作该功能走通，F12 无报错     |
| 部署到生产         | 浏览器打开生产地址，登录 → 进首页 → 点核心页面全部走通 |
| 改了接口           | 不只测该接口，还要测**调用它的前端页面**在浏览器里正常 |

### 铁律

1. **区分并如实报告验证级别**：进程存活(L1) / 接口可达(L2) / 浏览器端到端(L3)。只有 L3 通过才可说"完成、可使用"。
2. **禁止用 L1/L2 冒充 L3**：不得在只做了 curl 的情况下宣布"可以用了"。
3. **有未验证项必须明说**："X 已验证，Y 未验证"，不含糊。
4. 详细规范见 [测试规范.md](07-测试规范/测试规范.md)、[更新流程.md](03-部署运维/更新流程.md)。


## 项目简介

教育信息化项目全生命周期管理平台，涵盖设备管理、任务计划、交付跟踪、培训管理、风险管理、报告生成等核心功能。

### 技术栈

| 层     | 技术选型                               |
| ------ | -------------------------------------- |
| 前端   | React 18 + TypeScript + Ant Design 5.x |
| 后端   | Python 3.11+ + FastAPI + SQLAlchemy    |
| 数据库 | MySQL 8.0                              |
| 部署   | Ubuntu 24.04 + Nginx + systemd         |

详细版本号见 [技术架构说明.md](02-架构设计/技术架构说明.md)。

### 默认账号

| 账号  | 密码       | 角色     |
| ----- | ---------- | -------- |
| admin | Admin@2026 | 管理员   |
| pm001 | Admin@2026 | 项目经理 |


## 快速启动

**后端**:

```bash
cd backend && uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**前端** (PowerShell):

```powershell
cd frontend; $env:PATH="C:\Program Files\nodejs;$env:PATH"; $env:BROWSER='none'; npm start
```

**访问**: http://localhost:3000 (前端) | http://127.0.0.1:8000 (后端API) | 登录: admin / Admin@2026

完整启动流程见 **[本地启动手册.md](本地启动手册.md)**。


## 核心约定

### 数据库枚举值

权威来源以 [CLAUDE.md](CLAUDE.md) 的"数据库枚举值"章节和 `ddl.sql` 为准，此处不重复维护。

### 菜单顺序

左侧导航栏菜单及系统管理子菜单见 [全局设计.md §二](02-架构设计/全局设计.md)。

> **学校看板** 与 **培训管理** 已整合进"交付进展"Tab，不再作为独立一级菜单（2026-07-26 定稿）。

### 数据可下钻

所有列表、统计数字、卡片、图表等数据内容都必须支持下钻。详细交互规范见 [全局设计.md §三](02-架构设计/全局设计.md)。


## 架构决策

### 前后端通信
- **开发环境**：前端直接调后端 `http://127.0.0.1:8000`（`.env.development` 配置）
- **生产环境**：前端用**相对路径**（`REACT_APP_API_URL` 置空），请求经 **Nginx 反代**到后端（同源，无 CORS）
- 前端统一API客户端: `src/services/api.ts`、`src/services/wbsService.ts`，勿硬编码地址

> ⚠️ 关键约束（生产留空、CORS、Node 18）已收录到 [CLAUDE.md](CLAUDE.md) 的"架构约定"章节。

### 数据库配置
- **服务器**: 124.222.151.69:3306
- **开发库**: gx_project_dev
- **生产库**: gx_project
- **表数量**: 19张

### 环境变量

**后端 `.env`**:
```env
DB_HOST=124.222.151.69
DB_PORT=3306
DB_USER=root
DB_PASSWORD=GX2026!root
DB_NAME=gx_project_dev
```

**前端**: 开发 `.env.development` 填 `REACT_APP_API_URL=http://127.0.0.1:8000`；
生产 `.env.production` **留空**（相对路径走 Nginx 反代）。


## 数据库表结构

完整表结构定义及 DDL 见 [数据库设计.md](02-架构设计/数据库设计.md)。

### 核心表清单（19张）

| 表名                | 说明              |
| ------------------- | ----------------- |
| users               | 用户表            |
| schools             | 学校表            |
| suppliers           | 供应商表          |
| templates           | 模板表            |
| template_wbs_stages | 模板关联WBS阶段表 |
| dict_items          | 数据字典表        |
| project_info        | 项目信息表        |
| operation_logs      | 操作日志表        |
| wbs_tasks           | WBS任务表         |
| task_attachments    | 任务佐证材料表    |
| devices             | 设备信息表        |
| trainings           | 培训计划表        |
| risks               | 风险管理表        |
| reports             | 报告管理表        |
| files               | 交付材料库表      |
| production_lines    | 产线类型表        |
| systems             | 系统字典表        |
| software_modules    | 软件模块表        |
| todos               | 待办表            |

> 已删除表（勿引用）：`risk_tasks`、`device_systems`、`training_schools`、`templates_old_20260728`。

### 关键字段差异

易错字段名权威列表见 [CLAUDE.md](CLAUDE.md) "易错字段名"章节。
字段名一律以 `ddl.sql` 与代码模型为准。


## API接口规范

### 响应格式

当前接口均返回**裸 JSON 对象，无统一包装层**（如 `{"items": [...]}`、`{"total": n, "items": [...]}`、`{"access_token": "..."}`）。新增接口沿用现状，勿假设存在 `code/data/message` 包装。

### 认证方式
- **Header**: `Authorization: Bearer <token>`
- Token有效期: 1440分钟

### 核心API路径

| 模块       | 路径前缀            | 说明               |
| ---------- | ------------------- | ------------------ |
| 认证       | `/api/v1/auth`      | 登录/登出/修改密码 |
| 用户       | `/api/v1/users`     | 用户CRUD           |
| 学校       | `/api/v1/schools`   | 学校CRUD           |
| Dashboard  | `/api/v1/dashboard` | 首页数据           |
| WBS        | `/api/v1/wbs-tasks` | 任务管理           |
| 设备信息   | `/api/v1/devices`   | 设备台账           |
| 交付进展   | `/api/v1/delivery`  | 交付进展           |
| 风险管理   | `/api/v1/risks`     | 风险管理           |
| 报告管理   | `/api/v1/reports`   | 报告管理           |
| 交付材料库 | `/api/v1/files`     | 文件管理           |


## 开发进度

### ✅ 已完成（截至2026-07-20）

| 模块             | 完成日期   | 说明                                   |
| ---------------- | ---------- | -------------------------------------- |
| 1-1 数据库初始化 | 2026-07-17 | 16张表，admin账号，数据字典            |
| 1-2 用户认证     | 2026-07-17 | JWT Token，登录/登出/改密码            |
| 1-3 用户管理     | 2026-07-17 | CRUD + 重置密码 + 前端页面             |
| Dashboard 首页   | 2026-07-20 | 7卡片 + 3环形图 + 里程碑 + 风险 + 待办 |

### 📅 待开发

| 模块                            | 优先级 | 说明                                     |
| ------------------------------- | ------ | ---------------------------------------- |
| 学校管理                        | P0     | 列表/新增/编辑/删除 + 重点学校标记       |
| 供应商管理                      | P0     | 供应商主数据维护                         |
| 模板管理                        | P0     | 标准文件模板上传、管理与下载             |
| 数据字典维护                    | P0     | 系统下拉选项统一维护（含自动生成功能）   |
| 项目信息管理                    | P0     | 项目基本信息与整体状态管理               |
| 项目计划（WBS）                 | P1     | 含自动生成逻辑                           |
| 设备信息                        | P1     | 设备台账（含设备规格管理入口）           |
| 交付进展（含学校看板+培训管理） | P2     | 各学校交付状态、单校全景、培训计划与材料 |
| 风险管理                        | P2     | 风险列表与跟踪（轻量级模型）             |
| 报告管理                        | P3     | 周报/月报                                |
| 交付材料库                      | P3     | 文件管理                                 |
| 项目复盘                        | P3     | 验收后复盘                               |
| 操作日志                        | P3     | 系统操作记录与审计                       |


## 重要踩坑记录

高频踩坑速记见 [CLAUDE.md](CLAUDE.md)（架构约定、MySQL规范等），本章为详细版。

### MySQL中文双重编码问题 ⚠️

**问题根因**：导入SQL时未指定字符集，导致ENUM中文值被双重编码。

**受影响字段**（已修复）：software_modules.phase、todos.priority、todos.status

**预防方法**：

**必须使用封装脚本导入**，不要手敲 mysql 命令：

```bash
# PowerShell
.\scripts\import_sql.ps1 ddl.sql

# Bash
./scripts/import_sql.sh ddl.sql
```

脚本自动添加 `--default-character-set=utf8mb4`，防止双重编码。

**手动导入（如必须）**：
```bash
docker exec -i gx_mysql mysql -uroot -pGX2026!root \
  --default-character-set=utf8mb4 gx_project_dev < ddl.sql
```

> 历史教训：P008/P009/P010 均因导入时未加字符集参数导致。

### CORS配置
```python
# 不能同时使用 allow_origins=["*"] 和 allow_credentials=True
allow_origins=['http://localhost:3000', 'http://127.0.0.1:3000']
allow_credentials=True
```

### uvicorn --reload 不可靠
改完后端代码若行为没变，手动 kill 所有 python 进程重启。

### Node环境变量
前端启动必须用 PowerShell 并手动添加 Node 到 PATH：
```powershell
$env:PATH = "C:\Program Files\nodejs;$env:PATH"
```

### JWT Token格式
JWT的 `sub` 字段使用 **user.id**（不是 username）。


## Git工作流

详细提交规范（格式、type、scope、body、footer、分支命名、常用命令）见 **[Git提交规范.md](04-开发规范/Git提交规范.md)**。

### 快速参考

```bash
git add <files>
git commit -m "type(scope): subject"
git push origin main
```

### 提交类型速查

| type     | 说明              |
| -------- | ----------------- |
| feat     | 新功能            |
| fix      | Bug修复           |
| docs     | 文档变更          |
| style    | 代码格式          |
| refactor | 代码重构          |
| perf     | 性能优化          |
| test     | 测试相关          |
| chore    | 构建工具/依赖更新 |

> 详细 type/scope 列表见 [Git提交规范.md](04-开发规范/Git提交规范.md)。

### 推送流程

`git add` / `git commit` / `git push` 均由 Claude 直接执行（SSH已配）。**push 前必须先向用户说明提交内容并获确认**。

### 最近提交

```
d173bd6 fix(auth): 修复CORS配置、旧token兼容与antd警告
ac75939 feat(user): 完成用户管理模块（CRUD + 重置密码）
3a5bc27 chore: 项目架构规范化与环境配置
```


## 文档索引

> **本 README 是基准文档，先读它。**

### 按场景查文档

| 我要做什么                       | 先读这个文档                                           |
| -------------------------------- | ------------------------------------------------------ |
| 了解项目全貌、约定、踩坑         | 本 README                                              |
| 开发新功能前，看需求             | [01-需求文档/](01-需求文档/)                           |
| 设计表结构 / 查字段              | [数据库设计.md](02-架构设计/数据库设计.md) + ddl.sql   |
| 理解系统架构、技术选型           | [技术架构说明.md](02-架构设计/技术架构说明.md)         |
| 看页面骨架、菜单、交互规范       | [全局设计.md](02-架构设计/全局设计.md)                 |
| 开发前看施工图                   | [详细设计开发文档.md](02-架构设计/详细设计开发文档.md) |
| 业务逻辑（进度计算、状态判断等） | [业务规则说明.md](02-架构设计/业务规则说明.md)         |
| 写代码前看编码规范               | [代码规范.md](04-开发规范/代码规范.md)                 |
| **提交/推送代码**                | [Git提交规范.md](04-开发规范/Git提交规范.md)           |
| **开发完成后做测试**             | [测试规范.md](07-测试规范/测试规范.md)                 |
| **部署/更新到生产**              | [更新流程 SOP](03-部署运维/更新流程.md)                |
| 首次部署 / 环境搭建              | [部署手册.md](03-部署运维/部署手册.md)                 |
| 线上出问题排查                   | [问题排查SOP.md](03-部署运维/问题排查SOP.md)           |
| 架构/数据库变更历史              | [变更记录.md](10-变更记录/变更记录.md)                 |

### 完整目录

| 目录     | 位置         |
| -------- | ------------ |
| 环境配置 | 00-环境配置/ |
| 需求文档 | 01-需求文档/ |
| 架构设计 | 02-架构设计/ |
| 部署运维 | 03-部署运维/ |
| 开发规范 | 04-开发规范/ |
| 性能优化 | 05-性能优化/ |
| 原型设计 | 06-原型设计/ |
| 测试规范 | 07-测试规范/ |
| 技术方案 | 07-技术方案/ |
| 历史归档 | 08-历史归档/ |
| 工作日志 | 09-工作日志/ |
| 变更记录 | 10-变更记录/ |


## 快速检查清单

### 启动前
- [ ] Node 版本是 18.x（`node -v`）
- [ ] 后端 `.env` 配置正确（DB_HOST=124.222.151.69）
- [ ] 前端 `.env.development` 存在

### 问题修复后
- [ ] L3浏览器端到端验证通过
- [ ] **登记到问题簿**: `python scripts/log_issue.py`

### 验证（必须走到 L3 端到端才算完成）

验证分3个级别：L1服务存活 / L2接口连通 / L3浏览器端到端。**只有 L3 通过才算"完成/可用"**。

> ⚠️ 只做 curl(L2) 不算验证完成。curl 测不出 CORS/前端报错。


## 已知问题（待修复）

### 1. Device 模型字段名与数据库不匹配

| models.py         | 数据库实际列名 |
| ----------------- | -------------- |
| installation_date | install_date   |
| debugging_date    | debug_date     |
| acceptance_date   | accept_date    |

**影响**：只要有功能 SELECT/INSERT 这些字段就会抛 500 错误。推荐方案：改 models.py 对齐数据库。

### 2. Dashboard 统计逻辑待优化

| 项                | 当前逻辑                      | 建议优化                           |
| ----------------- | ----------------------------- | ---------------------------------- |
| 重点学校卡片显示0 | 演示数据所有 schools.is_key=0 | 标记1-2所学校为重点                |
| 设备类型数=180    | 统计逻辑可能不准              | 改为 `COUNT(DISTINCT device_name)` |


## 技术支持

- **服务器**: ssh ubuntu@124.222.151.69
- **MySQL**: docker exec -it gx_mysql mysql -uroot -pGX2026!root gx_project

**维护者**: GX-PM Team
**最后更新**: 2026-07-29
**版本**: V2.2
