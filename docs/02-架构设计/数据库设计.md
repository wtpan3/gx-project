```markdown
# GX教育项目交付管理系统 - 数据库设计

**版本**：V1.0
**日期**：2026年7月


## 一、ER图（核心表关系）

```
┌─────────────┐          ┌─────────────┐
│   users     │          │   schools   │
├─────────────┤          ├─────────────┤
│ id          │◄─────────│ id          │
│ username    │          │ code        │
│ password_hash│         │ full_name   │
│ real_name   │          │ region      │
│ role        │          │ address     │
│ phone       │          │ campus_manager_id│
│ email       │          │ contact_person│
│ status      │          │ contact_phone│
│ created_at  │          │ project_status│
│ updated_at  │          │ remark      │
└─────────────┘          │ created_at  │
         │               │ updated_at  │
         │               └─────────────┘
         │                      │
         │                      │
         ▼                      ▼
┌─────────────┐          ┌─────────────┐
│device_systems│          │  wbs_tasks  │
├─────────────┤          ├─────────────┤
│ id          │          │ id          │
│ project_name│          │ project_phase_l1│
│ construction_year│     │ sub_phase_l2│
│ system_name │          │ task_package_l3│
│ device_name │          │ work_content_l4│
│ brand       │          │ stage_type  │
│ model       │          │ plan_start_date│
│ params      │          │ plan_end_date│
│ type        │          │ status      │
│ unit        │          │ assignee_id │───┐
│ plan_quantity│         │ school_id   │───┼──┐
│ is_enabled  │          │ source_device_id│ │  │
│ created_at  │          │ is_orphan   │   │  │
│ updated_at  │          └─────────────┘   │  │
└─────────────┘                  │         │  │
         │                       │         │  │
         ▼                       ▼         │  │
┌─────────────┐          ┌─────────────┐   │  │
│   devices   │          │  trainings  │   │  │
├─────────────┤          ├─────────────┤   │  │
│ id          │          │ id          │   │  │
│ system_id   │───────── │ type        │   │  │
│ device_name │          │ content     │   │  │
│ brand       │          │ status      │   │  │
│ model       │          │ school_id   │───┘  │
│ source      │          │ related_task_id───┘
│ quantity    │          │ is_district │
│ school_id   │──────────│ created_at  │
│ status      │          │ updated_at  │
│ supplier_id │          └─────────────┘
│ plan_arrival_date│
│ created_at  │
│ updated_at  │
└─────────────┘

```

## 二、完整表结构

### 2.1 users - 用户表

| 字段 | 类型 | 说明 | 必填 | 唯一 |
|------|------|------|------|------|
| id | INT | 主键，自增 | 是 | 是 |
| username | VARCHAR(50) | 登录账号 | 是 | 是 |
| password_hash | VARCHAR(255) | bcrypt加密密码 | 是 | 否 |
| real_name | VARCHAR(50) | 真实姓名 | 是 | 否 |
| role | ENUM | admin/project_manager/campus_manager | 是 | 否 |
| phone | VARCHAR(20) | 手机号 | 是 | 是 |
| email | VARCHAR(100) | 邮箱 | 是 | 否 |
| status | ENUM | 启用/停用，默认"启用" | 否 | 否 |
| created_at | DATETIME | 创建时间 | 否 | 否 |
| updated_at | DATETIME | 更新时间 | 否 | 否 |

**SQL建表语句：**
```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL COMMENT '登录账号',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    real_name VARCHAR(50) NOT NULL COMMENT '真实姓名',
    role ENUM('admin','project_manager','campus_manager') NOT NULL COMMENT '角色',
    phone VARCHAR(20) UNIQUE NOT NULL COMMENT '手机号',
    email VARCHAR(100) NOT NULL COMMENT '邮箱',
    status ENUM('启用','停用') DEFAULT '启用' COMMENT '状态',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**默认管理员账号：**
| 字段          | 值                                                           |
| ------------- | ------------------------------------------------------------ |
| username      | admin                                                        |
| password_hash | $2b$12$raNb1MQCp8eBVZwFIvM7wOh66yFySULvnRz4N5iSgh0xILGXRFb4u |
| real_name     | 系统管理员                                                   |
| role          | admin                                                        |
| phone         | 13800000000                                                  |
| email         | admin@gx.com                                                 |
| status        | 启用                                                         |


### 2.2 schools - 学校表

| 字段              | 类型         | 说明                               | 必填 | 唯一 |
| ----------------- | ------------ | ---------------------------------- | ---- | ---- |
| id                | INT          | 主键，自增                         | 是   | 是   |
| code              | VARCHAR(50)  | 学校编码，如GX-2025-001            | 是   | 是   |
| full_name         | VARCHAR(200) | 学校全称                           | 是   | 否   |
| region            | VARCHAR(100) | 所属区域（省/市/区）               | 是   | 否   |
| address           | VARCHAR(255) | 详细地址                           | 否   | 否   |
| campus_manager_id | INT          | 校园经理ID，关联users.id           | 否   | 否   |
| contact_person    | VARCHAR(50)  | 校方联系人姓名                     | 是   | 否   |
| contact_phone     | VARCHAR(20)  | 校方联系人电话                     | 是   | 否   |
| project_status    | ENUM         | 未启动/实施中/试运行/已验收/维保中 | 否   | 否   |
| is_key            | TINYINT      | 是否重点学校：1=是，0=否           | 否   | 否   |
| remark            | TEXT         | 备注                               | 否   | 否   |
| created_at        | DATETIME     | 创建时间                           | 否   | 否   |
| updated_at        | DATETIME     | 更新时间                           | 否   | 否   |

**SQL建表语句：**
```sql
CREATE TABLE schools (
    id INT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '学校编码',
    full_name VARCHAR(200) NOT NULL COMMENT '学校全称',
    region VARCHAR(100) NOT NULL COMMENT '所属区域',
    address VARCHAR(255) COMMENT '详细地址',
    campus_manager_id INT COMMENT '校园经理ID，关联users.id',
    contact_person VARCHAR(50) NOT NULL COMMENT '校方联系人',
    contact_phone VARCHAR(20) NOT NULL COMMENT '校方联系电话',
    project_status ENUM('未启动','实施中','试运行','已验收','维保中') DEFAULT '未启动' COMMENT '项目状态',
    is_key TINYINT DEFAULT 0 COMMENT '是否重点学校：1=是，0=否',
    remark TEXT COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (campus_manager_id) REFERENCES users(id) ON DELETE SET NULL
);
```


### 2.3 device_systems - 设备系统字典表

| 字段              | 类型         | 说明                               | 必填 | 唯一      |
| ----------------- | ------------ | ---------------------------------- | ---- | --------- |
| id                | INT          | 主键，自增                         | 是   | 是        |
| project_name      | VARCHAR(100) | 项目名称                           | 是   | 组合唯一① |
| construction_year | INT          | 建设年份（2026/2027/...）          | 是   | 组合唯一① |
| system_name       | VARCHAR(100) | 系统名称                           | 是   | 组合唯一① |
| device_name       | VARCHAR(100) | 设备名称                           | 是   | 组合唯一① |
| brand             | VARCHAR(100) | 品牌                               | 是   | 否        |
| model             | VARCHAR(100) | 型号                               | 是   | 否        |
| params            | TEXT         | 技术参数                           | 否   | 否        |
| type              | ENUM         | 硬件/软件/服务                     | 是   | 否        |
| source            | ENUM         | 设备来源：自研/外采，默认"外采"     | 否   | 否        |
| unit              | VARCHAR(20)  | 台/套/个                           | 是   | 否        |
| plan_quantity     | INT          | 计划总数量                         | 是   | 否        |
| is_enabled        | TINYINT      | 是否启用（软删除），1=启用，0=停用 | 否   | 否        |
| created_at        | DATETIME     | 创建时间                           | 否   | 否        |
| updated_at        | DATETIME     | 更新时间                           | 否   | 否        |

> ① 组合唯一：`project_name + system_name + device_name + construction_year` 四者组合唯一

**SQL建表语句：**
```sql
CREATE TABLE device_systems (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_name VARCHAR(100) NOT NULL COMMENT '项目名称',
    construction_year INT NOT NULL COMMENT '建设年份',
    system_name VARCHAR(100) NOT NULL COMMENT '系统名称',
    device_name VARCHAR(100) NOT NULL COMMENT '设备名称',
    brand VARCHAR(100) NOT NULL COMMENT '品牌',
    model VARCHAR(100) NOT NULL COMMENT '型号',
    params TEXT COMMENT '技术参数',
    type ENUM('硬件','软件','服务') NOT NULL COMMENT '设备类型',
    source ENUM('自研','外采') DEFAULT '外采' COMMENT '设备来源',
    unit VARCHAR(20) NOT NULL COMMENT '单位',
    plan_quantity INT NOT NULL COMMENT '计划数量',
    is_enabled TINYINT DEFAULT 1 COMMENT '是否启用(软删除)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_project_system_device_year (project_name, system_name, device_name, construction_year)
);
```


### 2.4 suppliers - 供应商表

| 字段           | 类型         | 说明            | 必填 | 唯一 |
| -------------- | ------------ | --------------- | ---- | ---- |
| id             | INT          | 主键，自增      | 是   | 是   |
| name           | VARCHAR(100) | 供应商/厂家全称 | 是   | 是   |
| contact_person | VARCHAR(50)  | 对接人姓名      | 是   | 否   |
| contact_phone  | VARCHAR(20)  | 联系电话        | 是   | 否   |
| contact_email  | VARCHAR(100) | 联系邮箱        | 否   | 否   |
| remark         | TEXT         | 备注            | 否   | 否   |
| created_at     | DATETIME     | 创建时间        | 否   | 否   |
| updated_at     | DATETIME     | 更新时间        | 否   | 否   |

**SQL建表语句：**
```sql
CREATE TABLE suppliers (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) UNIQUE NOT NULL COMMENT '供应商名称',
    contact_person VARCHAR(50) NOT NULL COMMENT '联系人',
    contact_phone VARCHAR(20) NOT NULL COMMENT '联系电话',
    contact_email VARCHAR(100) COMMENT '联系邮箱',
    remark TEXT COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```


### 2.5 templates - 模板表

| 字段        | 类型         | 说明                                                         | 必填 |
| ----------- | ------------ | ------------------------------------------------------------ | ---- |
| id          | INT          | 主键，自增                                                   | 是   |
| name        | VARCHAR(100) | 模板名称                                                     | 是   |
| type        | ENUM         | 到货验收表/加电测试表/校级验收单/培训确认表/培训签到表/培训反馈表/其他 | 是   |
| stage       | ENUM         | 到货验收/加电测试/校级验收/培训                              | 是   |
| file_path   | VARCHAR(500) | 文件存储路径                                                 | 是   |
| version     | VARCHAR(20)  | 版本号，如V1.0                                               | 是   |
| description | TEXT         | 说明                                                         | 否   |
| status      | ENUM         | 启用/停用，默认"启用"                                        | 否   |
| created_at  | DATETIME     | 创建时间                                                     | 否   |
| updated_at  | DATETIME     | 更新时间                                                     | 否   |

**SQL建表语句：**
```sql
CREATE TABLE templates (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL COMMENT '模板名称',
    type ENUM('到货验收表','加电测试表','校级验收单','培训确认表','培训签到表','培训反馈表','其他') NOT NULL COMMENT '模板类型',
    stage ENUM('到货验收','加电测试','校级验收','培训') NOT NULL COMMENT '关联阶段',
    file_path VARCHAR(500) NOT NULL COMMENT '文件路径',
    version VARCHAR(20) NOT NULL COMMENT '版本号',
    description TEXT COMMENT '说明',
    status ENUM('启用','停用') DEFAULT '启用' COMMENT '状态',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```


### 2.6 dict_items - 数据字典表

| 字段       | 类型         | 说明                     | 必填 |
| ---------- | ------------ | ------------------------ | ---- |
| id         | INT          | 主键，自增               | 是   |
| category   | VARCHAR(50)  | 字典分类                 | 是   |
| label      | VARCHAR(100) | 显示名称                 | 是   |
| value      | VARCHAR(100) | 值                       | 是   |
| sort_order | INT          | 排序，默认0              | 否   |
| is_enabled | TINYINT      | 是否启用，1=启用，0=停用 | 否   |
| created_at | DATETIME     | 创建时间                 | 否   |
| updated_at | DATETIME     | 更新时间                 | 否   |

**字典分类及初始数据：**

| category | label    | value    | sort_order |
| -------- | -------- | -------- | ---------- |
| 关联阶段 | 到货验收 | 到货验收 | 1          |
| 关联阶段 | 加电测试 | 加电测试 | 2          |
| 关联阶段 | 校级验收 | 校级验收 | 3          |
| 关联阶段 | 培训     | 培训     | 4          |
| 关联阶段 | 无       | 无       | 5          |
| 建设年份 | 2026     | 2026     | 1          |
| 建设年份 | 2027     | 2027     | 2          |
| 建设年份 | 2028     | 2028     | 3          |
| WBS状态  | 待开始   | 待开始   | 1          |
| WBS状态  | 进行中   | 进行中   | 2          |
| WBS状态  | 已完成   | 已完成   | 3          |
| WBS状态  | 已延期   | 已延期   | 4          |
| WBS状态  | 待补材料 | 待补材料 | 5          |
| L1阶段   | 启动规划 | 启动规划 | 1          |
| L1阶段   | 交付实施 | 交付实施 | 2          |
| L1阶段   | 验收移交 | 验收移交 | 3          |
| L1阶段   | 运营维护 | 运营维护 | 4          |

**SQL建表语句：**
```sql
CREATE TABLE dict_items (
    id INT PRIMARY KEY AUTO_INCREMENT,
    category VARCHAR(50) NOT NULL COMMENT '字典分类',
    label VARCHAR(100) NOT NULL COMMENT '显示名称',
    value VARCHAR(100) NOT NULL COMMENT '值',
    sort_order INT DEFAULT 0 COMMENT '排序',
    is_enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_category_value (category, value)
);
```


### 2.7 project_info - 项目信息表

| 字段           | 类型         | 说明                               | 必填 | 唯一 |
| -------------- | ------------ | ---------------------------------- | ---- | ---- |
| id             | INT          | 主键，自增                         | 是   | 是   |
| project_name   | VARCHAR(100) | 项目名称                           | 是   | 否   |
| project_code   | VARCHAR(50)  | 项目编码                           | 是   | 是   |
| start_date     | DATE         | 项目开始日期                       | 是   | 否   |
| end_date       | DATE         | 项目结束日期                       | 是   | 否   |
| overall_status | ENUM         | 未启动/进行中/试运行/已验收/已结项 | 否   | 否   |
| created_at     | DATETIME     | 创建时间                           | 否   | 否   |
| updated_at     | DATETIME     | 更新时间                           | 否   | 否   |

**SQL建表语句：**
```sql
CREATE TABLE project_info (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_name VARCHAR(100) NOT NULL COMMENT '项目名称',
    project_code VARCHAR(50) UNIQUE NOT NULL COMMENT '项目编码',
    start_date DATE NOT NULL COMMENT '项目开始日期',
    end_date DATE NOT NULL COMMENT '项目结束日期',
    overall_status ENUM('未启动','进行中','试运行','已验收','已结项') DEFAULT '未启动' COMMENT '项目整体状态',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```


### 2.8 wbs_tasks - WBS任务表

| 字段              | 类型         | 说明                                 | 必填 |
| ----------------- | ------------ | ------------------------------------ | ---- |
| id                | INT          | 主键，自增                           | 是   |
| project_phase_l1  | VARCHAR(50)  | L1项目阶段                           | 是   |
| sub_phase_l2      | VARCHAR(50)  | L2子阶段                             | 是   |
| task_package_l3   | VARCHAR(100) | L3工作任务包                         | 是   |
| work_content_l4   | VARCHAR(200) | L4工作内容（统计口径）               | 是   |
| work_detail_l5    | VARCHAR(200) | L5工作明细（可选）                   | 否   |
| stage_type        | ENUM         | 到货验收/加电测试/校级验收/培训/无   | 否   |
| plan_start_date   | DATE         | 计划开始时间                         | 是   |
| plan_end_date     | DATE         | 计划结束时间                         | 是   |
| status            | ENUM         | 待开始/进行中/已完成/已延期/待补材料 | 是   |
| actual_start_date | DATE         | 实际开始时间                         | 否   |
| actual_end_date   | DATE         | 实际结束时间                         | 否   |
| assignee_id       | INT          | 责任人，关联users.id                 | 是   |
| progress_note     | TEXT         | 进展说明                             | 否   |
| deliverables      | VARCHAR(255) | 输出物                               | 否   |
| school_id         | INT          | 关联学校，关联schools.id             | 是   |
| source_device_id  | INT          | 来源设备记录ID                       | 否   |
| construction_year | INT          | 建设年份                             | 否   |
| is_orphan         | TINYINT      | 是否孤儿任务，0=否，1=是             | 否   |
| created_at        | DATETIME     | 创建时间                             | 否   |
| updated_at        | DATETIME     | 更新时间                             | 否   |

**SQL建表语句：**
```sql
CREATE TABLE wbs_tasks (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_phase_l1 VARCHAR(50) NOT NULL COMMENT 'L1项目阶段',
    sub_phase_l2 VARCHAR(50) NOT NULL COMMENT 'L2子阶段',
    task_package_l3 VARCHAR(100) NOT NULL COMMENT 'L3工作任务包',
    work_content_l4 VARCHAR(200) NOT NULL COMMENT 'L4工作内容',
    work_detail_l5 VARCHAR(200) COMMENT 'L5工作明细',
    stage_type ENUM('到货验收','加电测试','校级验收','培训','无') COMMENT '关联阶段类型',
    plan_start_date DATE NOT NULL COMMENT '计划开始时间',
    plan_end_date DATE NOT NULL COMMENT '计划结束时间',
    status ENUM('待开始','进行中','已完成','已延期','待补材料') NOT NULL COMMENT '状态',
    actual_start_date DATE COMMENT '实际开始时间',
    actual_end_date DATE COMMENT '实际结束时间',
    assignee_id INT NOT NULL COMMENT '责任人',
    progress_note TEXT COMMENT '进展说明',
    deliverables VARCHAR(255) COMMENT '输出物',
    school_id INT NOT NULL COMMENT '关联学校',
    source_device_id INT COMMENT '来源设备记录ID',
    construction_year INT COMMENT '建设年份',
    is_orphan TINYINT DEFAULT 0 COMMENT '是否孤儿任务',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (assignee_id) REFERENCES users(id),
    FOREIGN KEY (school_id) REFERENCES schools(id)
);
```


### 2.9 devices - 设备信息表

| 字段              | 类型         | 说明                                      | 必填 |
| ----------------- | ------------ | ----------------------------------------- | ---- |
| id                | INT          | 主键，自增                                | 是   |
| project_name      | VARCHAR(100) | 项目名称                                  | 是   |
| construction_year | INT          | 建设年份                                  | 是   |
| system_id         | INT          | 关联device_systems.id                     | 是   |
| device_name       | VARCHAR(100) | 设备名称                                  | 是   |
| brand             | VARCHAR(100) | 品牌（从字典复制）                        | 是   |
| model             | VARCHAR(100) | 型号（从字典复制）                        | 是   |
| params            | TEXT         | 参数（从字典复制）                        | 否   |
| type              | ENUM         | 硬件/软件/服务（从字典复制）              | 是   |
| unit              | VARCHAR(20)  | 单位（从字典复制）                        | 是   |
| source            | ENUM         | 设备来源（库存发货/外部采购）             | 是   |
| quantity          | INT          | 该批次数量                                | 是   |
| school_id         | INT          | 分配学校，关联schools.id                  | 是   |
| install_location  | VARCHAR(100) | 安装位置                                  | 否   |
| status            | ENUM         | 待发货/已发货/已到货/已安装/已调试/已验收 | 是   |
| supplier_id       | INT          | 供应商，关联suppliers.id（仅外部采购）    | 否   |
| plan_arrival_date | DATE         | 到货计划时间                              | 是   |
| delivery_no       | VARCHAR(50)  | 发货单号                                  | 否   |
| delivery_date     | DATE         | 发货日期                                  | 否   |
| arrival_date      | DATE         | 到货日期                                  | 否   |
| install_date      | DATE         | 安装完成日期                              | 否   |
| debug_date        | DATE         | 调试完成日期                              | 否   |
| accept_date       | DATE         | 验收日期                                  | 否   |
| remark            | TEXT         | 备注                                      | 否   |
| created_at        | DATETIME     | 创建时间                                  | 否   |
| updated_at        | DATETIME     | 更新时间                                  | 否   |

**SQL建表语句：**
```sql
CREATE TABLE devices (
    id INT PRIMARY KEY AUTO_INCREMENT,
    project_name VARCHAR(100) NOT NULL COMMENT '项目名称',
    construction_year INT NOT NULL COMMENT '建设年份',
    system_id INT NOT NULL COMMENT '系统ID',
    device_name VARCHAR(100) NOT NULL COMMENT '设备名称',
    brand VARCHAR(100) NOT NULL COMMENT '品牌',
    model VARCHAR(100) NOT NULL COMMENT '型号',
    params TEXT COMMENT '技术参数',
    type ENUM('硬件','软件','服务') NOT NULL COMMENT '设备类型',
    unit VARCHAR(20) NOT NULL COMMENT '单位',
    source ENUM('库存发货','外部采购') NOT NULL COMMENT '设备来源',
    quantity INT NOT NULL COMMENT '数量',
    school_id INT NOT NULL COMMENT '分配学校',
    install_location VARCHAR(100) COMMENT '安装位置',
    status ENUM('待发货','已发货','已到货','已安装','已调试','已验收') NOT NULL COMMENT '当前状态',
    supplier_id INT COMMENT '供应商(仅外部采购)',
    plan_arrival_date DATE NOT NULL COMMENT '到货计划时间',
    delivery_no VARCHAR(50) COMMENT '发货单号',
    delivery_date DATE COMMENT '发货日期',
    arrival_date DATE COMMENT '到货日期',
    install_date DATE COMMENT '安装完成日期',
    debug_date DATE COMMENT '调试完成日期',
    accept_date DATE COMMENT '验收日期',
    remark TEXT COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (system_id) REFERENCES device_systems(id),
    FOREIGN KEY (school_id) REFERENCES schools(id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id)
);
```


### 2.10 trainings - 培训计划表

| 字段            | 类型         | 说明                                | 必填 |
| --------------- | ------------ | ----------------------------------- | ---- |
| id              | INT          | 主键，自增                          | 是   |
| type            | ENUM         | 集中培训/现场培训/线上培训/区级培训 | 是   |
| content         | VARCHAR(200) | 培训内容                            | 是   |
| target_audience | VARCHAR(100) | 参训对象                            | 是   |
| person_count    | INT          | 预计人数                            | 是   |
| duration_days   | DECIMAL(3,1) | 培训时长（天）                      | 是   |
| location        | VARCHAR(100) | 培训地点                            | 是   |
| method          | ENUM         | 理论讲授/实操演练/考核测评          | 是   |
| exam_method     | ENUM         | 笔试/实操/问卷/无                   | 是   |
| plan_date       | DATE         | 计划日期                            | 是   |
| actual_date     | DATE         | 实际日期                            | 否   |
| status          | ENUM         | 待培训/培训中/已完成/已取消         | 是   |
| related_task_id | INT          | 关联WBS任务ID                       | 否   |
| is_district     | TINYINT      | 是否区级培训，1=是，0=否            | 否   |
| created_at      | DATETIME     | 创建时间                            | 否   |
| updated_at      | DATETIME     | 更新时间                            | 否   |

**SQL建表语句：**
```sql
CREATE TABLE trainings (
    id INT PRIMARY KEY AUTO_INCREMENT,
    type ENUM('集中培训','现场培训','线上培训','区级培训') NOT NULL COMMENT '培训类型',
    content VARCHAR(200) NOT NULL COMMENT '培训内容',
    target_audience VARCHAR(100) NOT NULL COMMENT '参训对象',
    person_count INT NOT NULL COMMENT '预计人数',
    duration_days DECIMAL(3,1) NOT NULL COMMENT '培训天数',
    location VARCHAR(100) NOT NULL COMMENT '培训地点',
    method ENUM('理论讲授','实操演练','考核测评') NOT NULL COMMENT '培训方式',
    exam_method ENUM('笔试','实操','问卷','无') NOT NULL COMMENT '考核方式',
    plan_date DATE NOT NULL COMMENT '计划日期',
    actual_date DATE COMMENT '实际日期',
    status ENUM('待培训','培训中','已完成','已取消') NOT NULL COMMENT '培训状态',
    related_task_id INT COMMENT '关联WBS任务ID',
    is_district TINYINT DEFAULT 0 COMMENT '是否区级培训',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (related_task_id) REFERENCES wbs_tasks(id) ON DELETE SET NULL
);
```


### 2.11 training_schools - 培训学校关联表

| 字段        | 类型 | 说明                     | 必填 |
| ----------- | ---- | ------------------------ | ---- |
| id          | INT  | 主键，自增               | 是   |
| training_id | INT  | 培训ID，关联trainings.id | 是   |
| school_id   | INT  | 学校ID，关联schools.id   | 是   |

**SQL建表语句：**
```sql
CREATE TABLE training_schools (
    id INT PRIMARY KEY AUTO_INCREMENT,
    training_id INT NOT NULL COMMENT '培训ID',
    school_id INT NOT NULL COMMENT '学校ID',
    FOREIGN KEY (training_id) REFERENCES trainings(id) ON DELETE CASCADE,
    FOREIGN KEY (school_id) REFERENCES schools(id) ON DELETE CASCADE
);
```


### 2.12 risks - 风险管理表

| 字段                  | 类型     | 说明                 | 必填     |
| --------------------- | -------- | -------------------- | -------- |
| id                    | INT      | 主键，自增           | 是       |
| risk_desc             | TEXT     | 风险描述             | 是       |
| trigger_condition     | TEXT     | 触发条件             | 否       |
| impact_description    | TEXT     | 影响描述             | 否       |
| probability           | ENUM     | 高/中/低             | 是       |
| impact                | ENUM     | 高/中/低             | 是       |
| risk_level            | ENUM     | 高/中/低（自动计算） | 系统生成 |
| response_strategy     | TEXT     | 应对措施             | 否       |
| response_deadline     | DATE     | 应对计划完成时间     | 否       |
| responsible_person_id | INT      | 责任人，关联users.id | 是       |
| status                | ENUM     | 已识别/应对中/已关闭 | 是       |
| school_id             | INT      | 关联学校             | 否       |
| created_at            | DATETIME | 创建时间             | 否       |
| updated_at            | DATETIME | 更新时间             | 否       |

**风险等级自动计算规则：**

| 概率分值           | 影响分值           | 乘积 | 风险等级 |
| ------------------ | ------------------ | ---- | -------- |
| 高=3 / 中=2 / 低=1 | 高=3 / 中=2 / 低=1 | ≥6   | 高       |
| 高=3 / 中=2 / 低=1 | 高=3 / 中=2 / 低=1 | 4~5  | 中       |
| 高=3 / 中=2 / 低=1 | 高=3 / 中=2 / 低=1 | ≤3   | 低       |

**SQL建表语句：**
```sql
CREATE TABLE risks (
    id INT PRIMARY KEY AUTO_INCREMENT,
    risk_desc TEXT NOT NULL COMMENT '风险描述',
    trigger_condition TEXT COMMENT '触发条件',
    impact_description TEXT COMMENT '影响描述',
    probability ENUM('高','中','低') NOT NULL COMMENT '概率',
    impact ENUM('高','中','低') NOT NULL COMMENT '影响',
    risk_level ENUM('高','中','低') COMMENT '风险等级(自动计算)',
    response_strategy TEXT COMMENT '应对措施',
    response_deadline DATE COMMENT '应对计划完成时间',
    responsible_person_id INT NOT NULL COMMENT '责任人',
    status ENUM('已识别','应对中','已关闭') NOT NULL COMMENT '状态',
    school_id INT COMMENT '关联学校',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (responsible_person_id) REFERENCES users(id),
    FOREIGN KEY (school_id) REFERENCES schools(id) ON DELETE SET NULL
);
```


### 2.13 risk_tasks - 风险应对任务关联表

| 字段    | 类型 | 说明       | 必填 |
| ------- | ---- | ---------- | ---- |
| id      | INT  | 主键，自增 | 是   |
| risk_id | INT  | 风险ID     | 是   |
| task_id | INT  | WBS任务ID  | 是   |

**SQL建表语句：**
```sql
CREATE TABLE risk_tasks (
    id INT PRIMARY KEY AUTO_INCREMENT,
    risk_id INT NOT NULL COMMENT '风险ID',
    task_id INT NOT NULL COMMENT 'WBS任务ID',
    FOREIGN KEY (risk_id) REFERENCES risks(id) ON DELETE CASCADE,
    FOREIGN KEY (task_id) REFERENCES wbs_tasks(id) ON DELETE CASCADE
);
```


### 2.14 reports - 报告管理表

| 字段         | 类型         | 说明                           | 必填 |
| ------------ | ------------ | ------------------------------ | ---- |
| id           | INT          | 主键，自增                     | 是   |
| report_type  | ENUM         | week/month                     | 是   |
| report_scope | ENUM         | project/school                 | 是   |
| school_id    | INT          | 学校ID（当scope=school时必填） | 否   |
| period_start | DATE         | 周期开始日期                   | 是   |
| period_end   | DATE         | 周期结束日期                   | 是   |
| title        | VARCHAR(200) | 报告标题                       | 是   |
| content      | JSON         | 各章节内容快照                 | 是   |
| created_at   | DATETIME     | 创建时间                       | 否   |
| updated_at   | DATETIME     | 更新时间                       | 否   |

**SQL建表语句：**
```sql
CREATE TABLE reports (
    id INT PRIMARY KEY AUTO_INCREMENT,
    report_type ENUM('week','month') NOT NULL COMMENT '报告类型',
    report_scope ENUM('project','school') NOT NULL COMMENT '报告范围',
    school_id INT COMMENT '学校ID(scope=school时必填)',
    period_start DATE NOT NULL COMMENT '周期开始',
    period_end DATE NOT NULL COMMENT '周期结束',
    title VARCHAR(200) NOT NULL COMMENT '报告标题',
    content JSON NOT NULL COMMENT '内容快照',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (school_id) REFERENCES schools(id) ON DELETE CASCADE
);
```


### 2.15 files - 交付材料库表

| 字段          | 类型         | 说明                            | 必填 |
| ------------- | ------------ | ------------------------------- | ---- |
| id            | INT          | 主键，自增                      | 是   |
| file_name     | VARCHAR(255) | 文件名                          | 是   |
| file_path     | VARCHAR(500) | 文件存储路径                    | 是   |
| file_size     | INT          | 文件大小                        | 否   |
| file_type     | VARCHAR(50)  | 文件类型                        | 否   |
| source_module | ENUM         | project/training                | 是   |
| source_id     | INT          | 来源记录ID                      | 是   |
| school_id     | INT          | 关联学校                        | 否   |
| is_district   | TINYINT      | 是否区级培训材料                | 否   |
| stage_type    | ENUM         | 到货验收/加电测试/校级验收/培训 | 否   |
| upload_by     | INT          | 上传人，关联users.id            | 是   |
| upload_time   | DATETIME     | 上传时间                        | 否   |
| created_at    | DATETIME     | 创建时间                        | 否   |

**SQL建表语句：**
```sql
CREATE TABLE files (
    id INT PRIMARY KEY AUTO_INCREMENT,
    file_name VARCHAR(255) NOT NULL COMMENT '文件名',
    file_path VARCHAR(500) NOT NULL COMMENT '文件路径',
    file_size INT COMMENT '文件大小',
    file_type VARCHAR(50) COMMENT '文件类型',
    source_module ENUM('project','training') NOT NULL COMMENT '来源模块',
    source_id INT NOT NULL COMMENT '来源记录ID',
    school_id INT COMMENT '关联学校',
    is_district TINYINT DEFAULT 0 COMMENT '是否区级培训材料',
    stage_type ENUM('到货验收','加电测试','校级验收','培训') COMMENT '阶段类型',
    upload_by INT NOT NULL COMMENT '上传人',
    upload_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '上传时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (upload_by) REFERENCES users(id),
    FOREIGN KEY (school_id) REFERENCES schools(id) ON DELETE SET NULL
);
```


### 2.16 operation_logs - 操作日志表

| 字段                | 类型         | 说明                   | 必填 |
| ------------------- | ------------ | ---------------------- | ---- |
| id                  | BIGINT       | 主键，自增             | 是   |
| user_id             | INT          | 操作人ID，关联users.id | 是   |
| module              | VARCHAR(50)  | 所属模块               | 是   |
| action              | VARCHAR(50)  | 操作类型               | 是   |
| target_id           | INT          | 操作目标ID             | 否   |
| before_data         | JSON         | 变更前数据             | 否   |
| after_data          | JSON         | 变更后数据             | 否   |
| ip_address          | VARCHAR(45)  | IP地址                 | 否   |
| batch_file_name     | VARCHAR(255) | 批量导入文件名         | 否   |
| batch_success_count | INT          | 批量成功数             | 否   |
| batch_fail_count    | INT          | 批量失败数             | 否   |
| created_at          | DATETIME     | 操作时间               | 否   |

**SQL建表语句：**
```sql
CREATE TABLE operation_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL COMMENT '操作人ID',
    module VARCHAR(50) NOT NULL COMMENT '所属模块',
    action VARCHAR(50) NOT NULL COMMENT '操作类型',
    target_id INT COMMENT '操作目标ID',
    before_data JSON COMMENT '变更前数据',
    after_data JSON COMMENT '变更后数据',
    ip_address VARCHAR(45) COMMENT 'IP地址',
    batch_file_name VARCHAR(255) COMMENT '批量导入文件名',
    batch_success_count INT COMMENT '批量成功数',
    batch_fail_count INT COMMENT '批量失败数',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```


## 三、关键约束与索引

### 3.1 唯一约束

| 表             | 约束字段                                                    | 类型   |
| -------------- | ----------------------------------------------------------- | ------ |
| users          | username                                                    | UNIQUE |
| users          | phone                                                       | UNIQUE |
| schools        | code                                                        | UNIQUE |
| device_systems | (project_name, system_name, device_name, construction_year) | UNIQUE |
| suppliers      | name                                                        | UNIQUE |
| project_info   | project_code                                                | UNIQUE |
| dict_items     | (category, value)                                           | UNIQUE |

### 3.2 外键约束

| 表               | 外键字段              | 关联表         | ON DELETE |
| ---------------- | --------------------- | -------------- | --------- |
| schools          | campus_manager_id     | users          | SET NULL  |
| wbs_tasks        | assignee_id           | users          | RESTRICT  |
| wbs_tasks        | school_id             | schools        | RESTRICT  |
| devices          | system_id             | device_systems | RESTRICT  |
| devices          | school_id             | schools        | RESTRICT  |
| devices          | supplier_id           | suppliers      | SET NULL  |
| trainings        | related_task_id       | wbs_tasks      | SET NULL  |
| training_schools | training_id           | trainings      | CASCADE   |
| training_schools | school_id             | schools        | CASCADE   |
| risks            | responsible_person_id | users          | RESTRICT  |
| risks            | school_id             | schools        | SET NULL  |
| risk_tasks       | risk_id               | risks          | CASCADE   |
| risk_tasks       | task_id               | wbs_tasks      | CASCADE   |
| reports          | school_id             | schools        | CASCADE   |
| files            | upload_by             | users          | RESTRICT  |
| files            | school_id             | schools        | SET NULL  |
| operation_logs   | user_id               | users          | RESTRICT  |


### 3.1 V2.1新增表

#### 3.1.1 production_lines - 产线类型表

| 字段          | 类型         | 说明                     | 必填 | 唯一 |
| ------------- | ------------ | ------------------------ | ---- | ---- |
| id            | INT          | 主键，自增               | 是   | 是   |
| code          | VARCHAR(50)  | 产线编码                 | 是   | 是   |
| name          | VARCHAR(100) | 产线名称                 | 是   | 否   |
| system_id     | INT          | 所属系统ID               | 否   | 否   |
| description   | TEXT         | 描述                     | 否   | 否   |
| created_at    | DATETIME     | 创建时间                 | 否   | 否   |
| updated_at    | DATETIME     | 更新时间                 | 否   | 否   |

**SQL建表语句：**
```sql
CREATE TABLE production_lines (
    id INT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '产线编码',
    name VARCHAR(100) NOT NULL COMMENT '产线名称',
    system_id INT COMMENT '所属系统ID',
    description TEXT COMMENT '描述',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 3.1.2 software_modules - 软件模块表

| 字段                | 类型         | 说明                                                         | 必填 | 唯一 |
| ------------------- | ------------ | ------------------------------------------------------------ | ---- | ---- |
| id                  | INT          | 主键，自增                                                   | 是   | 是   |
| name                | VARCHAR(100) | 模块名称                                                     | 是   | 否   |
| code                | VARCHAR(50)  | 模块编码                                                     | 是   | 是   |
| description         | TEXT         | 描述                                                         | 否   | 否   |
| current_phase       | ENUM         | 需求收集中/需求确认中/开发中/测试中/部署中/运行中，默认"需求收集中" | 否   | 否   |
| planned_completion  | DATE         | 计划完成时间                                                 | 否   | 否   |
| actual_completion   | DATE         | 实际完成时间                                                 | 否   | 否   |
| created_at          | DATETIME     | 创建时间                                                     | 否   | 否   |
| updated_at          | DATETIME     | 更新时间                                                     | 否   | 否   |

**SQL建表语句：**
```sql
CREATE TABLE software_modules (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL COMMENT '模块名称',
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '模块编码',
    description TEXT COMMENT '描述',
    current_phase ENUM('需求收集中','需求确认中','开发中','测试中','部署中','运行中') DEFAULT '需求收集中',
    planned_completion DATE COMMENT '计划完成时间',
    actual_completion DATE COMMENT '实际完成时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 3.1.3 todos - 待办表

| 字段               | 类型         | 说明                                       | 必填 | 唯一 |
| ------------------ | ------------ | ------------------------------------------ | ---- | ---- |
| id                 | INT          | 主键，自增                                 | 是   | 是   |
| parent_id          | INT          | 父待办ID，支持多级树形结构，NULL 为顶级   | 否   | 否   |
| title              | VARCHAR(200) | 任务标题                                   | 是   | 否   |
| description        | TEXT         | 任务描述                                   | 否   | 否   |
| priority           | ENUM         | 高/中/低，默认"中"                         | 否   | 否   |
| due_date           | DATE         | 截止日期                                   | 否   | 否   |
| status             | ENUM         | 待处理/已完成，默认"待处理"                 | 否   | 否   |
| assignee_id        | INT          | 负责人ID，关联users.id                     | 否   | 否   |
| creator_id         | INT          | 创建人ID，关联users.id                     | 否   | 否   |
| source_type        | ENUM         | 来源类型：project/wbs/system，默认"project" | 否   | 否   |
| source_id          | INT          | 来源记录ID                                 | 否   | 否   |
| transferred_from_id| INT          | 转办来源人ID（用于转办记录）               | 否   | 否   |
| completed_at       | DATETIME     | 完成时间                                   | 否   | 否   |
| created_at         | DATETIME     | 创建时间                                   | 否   | 否   |
| updated_at         | DATETIME     | 更新时间                                   | 否   | 否   |

**末级待办联动规则**：
- 当一个待办的所有子待办（parent_id = 该待办ID）状态全部为"已完成"时，该待办自动更新为"已完成"
- 首页和个人待办列表仅展示末级待办（没有子待办的待办，即 NOT EXISTS 子记录）

**SQL建表语句：**
```sql
CREATE TABLE todos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    parent_id INT COMMENT '父待办ID，支持多级树形结构，NULL为顶级',
    title VARCHAR(200) NOT NULL COMMENT '任务标题',
    description TEXT COMMENT '任务描述',
    priority ENUM('高','中','低') DEFAULT '中',
    due_date DATE COMMENT '截止日期',
    status ENUM('待处理','已完成') DEFAULT '待处理',
    assignee_id INT COMMENT '负责人ID',
    creator_id INT COMMENT '创建人ID',
    source_type ENUM('project','wbs','system') DEFAULT 'project',
    source_id INT COMMENT '来源记录ID',
    transferred_from_id INT COMMENT '转办来源人ID（用于转办记录）',
    completed_at DATETIME COMMENT '完成时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_id) REFERENCES todos(id) ON DELETE CASCADE,
    FOREIGN KEY (assignee_id) REFERENCES users(id),
    FOREIGN KEY (creator_id) REFERENCES users(id),
    INDEX idx_parent (parent_id),
    INDEX idx_assignee (assignee_id),
    INDEX idx_status (status)
);
```


## 四、版本信息

| 项目       | 版本       |
| ---------- | ---------- |
| 文档版本   | V1.0       |
| 数据库版本 | MySQL 8.0  |
| 最后更新   | 2026-07-20 |
