# GX项目本地开发环境启动手册

**版本**：V1.1
**最后更新**：2026-07-29

> 本文档为本地开发环境详细启动指南。快速启动命令详见 [README.md](README.md) §快速启动。


## 前置准备

### 1. 安装 Docker Desktop（Windows）

**官方下载**：https://www.docker.com/products/docker-desktop/

**国内镜像**（更快）：
- 阿里云：https://developer.aliyun.com/mirror/docker-ce
- 腾讯云：https://mirrors.cloud.tencent.com/docker-ce/

下载后双击安装，重启电脑，确认Docker启动成功（任务栏右下角有Docker图标）。

### 2. 确认环境

```bash
# 检查版本
python --version    # Python 3.11+
node --version      # 必须使用 Node 18.x LTS（不支持20+）
docker --version    # 安装Docker后执行
```

> ⚠️ **Node版本警告**：本项目前端使用 React 18，必须使用 Node 18.x LTS。使用 Node 20+ 会导致依赖安装失败或运行时错误。如已安装高版本，建议使用 nvm 切换到 18.x。


## 启动步骤

### 第一步：启动 MySQL

```bash
cd "f:/claude code/Projectmanage/gx-project"
docker-compose up -d
```

等待 MySQL 容器启动完成（约 30 秒），确认状态：

```bash
docker ps
# 应该看到 gx_mysql 容器 STATUS 为 Up
```

### 第二步：初始化数据库

> ⚠️ **重要**：必须使用封装脚本导入 SQL，自动添加 `--default-character-set=utf8mb4` 防止中文双重编码（历史血泪教训 P008-P010）。

```bash
cd backend

# PowerShell
.\scripts\import_sql.ps1 ddl.sql

# 或 Git Bash
./scripts/import_sql.sh ddl.sql
```

看到导入完成提示，说明成功。

**默认账号**：
- 管理员：admin / Admin@2026
- 项目经理：pm001 / Admin@2026

### 第三步：启动后端

```bash
# 在 backend 目录下
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

看到 `Application startup complete` 说明后端启动成功。

浏览器访问 http://localhost:8000/health 应该看到 `{"status":"ok"}`。

### 第四步：启动前端

**新开一个终端窗口**：

```bash
cd "f:/claude code/Projectmanage/gx-project/frontend"

# 首次运行需要安装依赖
npm install

# 启动开发服务器
npm start
```

等待编译完成，浏览器会自动打开 http://localhost:3000，看到登录页面。


## 登录测试

用 `admin / Admin@2026` 登录，成功后会跳转到 Dashboard。


## 常见问题

### Docker 启动失败

- 确认 Docker Desktop 已启动（任务栏有图标）
- 确认端口 3306 未被占用：`netstat -ano | findstr :3306`

### 后端启动报错 "No module named 'app'"

确认在 `backend` 目录下执行 `uvicorn` 命令。

### 前端登录失败

1. 确认后端已启动（访问 http://localhost:8000/health）
2. 打开浏览器开发者工具（F12）查看 Network 标签页，检查请求是否正常

### 数据库中文乱码

- 确认初始化时使用了 `import_sql.ps1/sh` 脚本（自动加字符集参数）
- 如已出现乱码，参见 README.md §重要踩坑记录 → MySQL中文双重编码问题


## 停止服务

```bash
# 停止 MySQL
docker-compose down

# 停止后端和前端：各自终端按 Ctrl+C
```


## 文档索引

| 需要了解的内容    | 文档                                         |
| ----------------- | -------------------------------------------- |
| 快速启动命令      | [README.md](README.md) §快速启动             |
| MySQL中文编码问题 | [README.md](README.md) §重要踩坑记录         |
| 编码规范          | [代码规范.md](04-开发规范/代码规范.md)       |
| Git提交规范       | [Git提交规范.md](04-开发规范/Git提交规范.md) |
| 生产环境部署      | [更新流程.md](03-部署运维/更新流程.md)       |


## 版本信息

| 项目     | 版本       |
| -------- | ---------- |
| 文档版本 | V1.1       |
| 最后更新 | 2026-07-29 |
